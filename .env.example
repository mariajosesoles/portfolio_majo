# Cloudflare Web Analytics — token from dashboard (Analytics → Web Analytics → Add site)
# Set this in Cloudflare Pages → Settings → Environment variables (Production + Preview)
PUBLIC_CF_WEB_ANALYTICS_TOKEN=
