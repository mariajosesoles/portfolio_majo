---
import type { SiteConfig } from "../../types/site";
import { StackIcons } from "../ui/StackIcons";

interface Props {
	config: SiteConfig;
}

const { config } = Astro.props;
const os = config.openSource;
---

<section id="opensource" class="py-16 md:py-20">
	<div class="page-container">
		<h2 class="type-section-title mb-10">{config.sections.openSource}</h2>
		<article class="rounded-2xl border border-border bg-bg-elevated/50 p-6 md:p-8">
			<h3 class="type-h3 mb-1">{os.title}</h3>
			<p class="type-small mb-4 text-accent">{os.role}</p>
			<div class="mb-4">
				<StackIcons stack={os.stack} client:visible />
			</div>
			<p class="type-body mb-3 text-muted">{os.description}</p>
			<div class="flex flex-wrap items-center gap-4">
				{
					os.url && (
						<a
							href={os.url}
							target="_blank"
							rel="noopener noreferrer"
							class="type-small inline-flex items-center gap-1.5 font-medium text-accent hover:underline"
						>
							{config.cta.viewLive}
							<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
								<path d="M15 3h6v6" />
								<path d="M10 14 21 3" />
								<path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6" />
							</svg>
						</a>
					)
				}
				{os.pr && <p class="type-caption font-medium text-accent-2">{os.pr}</p>}
			</div>
		</article>
	</div>
</section>
