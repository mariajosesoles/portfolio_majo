# Cloudflare Pages (static Astro build). Deploy: .github/workflows/deploy.yml
name = "portfolio-majo"
compatibility_date = "2026-03-01"
pages_build_output_dir = "dist"

# Optional local preview: pnpm build && pnpm exec wrangler pages dev dist
